apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-realm-config
  namespace: apps
data:
  realm-today-events.json: |
    {
      "realm": "today-events",
      "enabled": true,
      "displayName": "Today Events",
      "note": "Realm template importato automaticamente. Configura i secret dei client dalla dashboard Keycloak."
    }
