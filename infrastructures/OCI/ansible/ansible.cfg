[defaults]
inventory = inventory.yml
host_key_checking = False
timeout = 30
remote_user = opc
# Decommentare per evitare --ask-vault-pass ad ogni esecuzione:
# vault_password_file = ~/.vault_pass

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
