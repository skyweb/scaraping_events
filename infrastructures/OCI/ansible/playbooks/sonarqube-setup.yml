---
# SonarQube — Code Quality & Security Analysis
# Installa SonarQube via Helm e applica i manifest K8s dalla directory k8s/sonarqube/
#
# Prerequisiti:
#   - install_sonarqube: true in oke.yml
#   - sonarqube_admin_password, sonarqube_postgres_password in oke-vault.yml
#   - PostgreSQL già installato nel namespace database (postgres.database.svc)
#   - kubectl e helm configurati
#
# Uso: ansible-playbook ansible/playbooks/sonarqube-setup.yml
- name: SonarQube Setup
  hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
    - ../vars/oke.yml
    - ../vars/oke-vault.yml

  vars:
    sonarqube_k8s_dir: "{{ playbook_dir }}/../../k8s/sonarqube"

  tasks:
    # =========================================================================
    # Verifica prerequisiti
    # =========================================================================
    - name: Verifica che install_sonarqube sia abilitato
      ansible.builtin.assert:
        that: install_sonarqube | default(false)
        fail_msg: "install_sonarqube non abilitato in oke.yml."

    - name: Verifica kubectl disponibile
      ansible.builtin.command: kubectl cluster-info
      register: cluster_info
      changed_when: false

    - name: Verifica Helm disponibile
      ansible.builtin.command: helm version --short
      register: helm_version
      changed_when: false

    # =========================================================================
    # Namespace
    # =========================================================================
    - name: Crea namespace devs
      ansible.builtin.command: kubectl create namespace devs
      register: ns_result
      changed_when: "'created' in ns_result.stdout"
      failed_when:
        - ns_result.rc != 0
        - "'already exists' not in ns_result.stderr"

    # =========================================================================
    # Database — Crea database sonarqube sul PostgreSQL esistente
    # =========================================================================
    - name: Crea database sonarqube su PostgreSQL esistente
      ansible.builtin.shell: |
        kubectl exec -n database statefulset/postgres -- \
          psql -U postgres -d postgres -c \
          "SELECT 'exists' FROM pg_database WHERE datname = 'sonarqube'" \
          | grep -q exists \
          || kubectl exec -n database statefulset/postgres -- \
          psql -U postgres -d postgres -c "CREATE DATABASE sonarqube"
      register: db_result
      changed_when: "'CREATE DATABASE' in db_result.stdout"

    - name: Crea utente sonarqube su PostgreSQL esistente
      ansible.builtin.shell: |
        kubectl exec -n database statefulset/postgres -- \
          psql -U postgres -d postgres -c \
          "SELECT 'exists' FROM pg_roles WHERE rolname = 'sonarqube'" \
          | grep -q exists \
          || kubectl exec -n database statefulset/postgres -- \
          psql -U postgres -d postgres -c \
          "CREATE USER sonarqube WITH PASSWORD '{{ sonarqube_postgres_password }}'; GRANT ALL PRIVILEGES ON DATABASE sonarqube TO sonarqube"
      register: user_result
      changed_when: "'CREATE ROLE' in user_result.stdout"

    # =========================================================================
    # Secret
    # =========================================================================
    - name: Crea Secret sonarqube-secret
      ansible.builtin.command:
        cmd: kubectl apply -f -
        stdin: |
          apiVersion: v1
          kind: Secret
          metadata:
            name: sonarqube-secret
            namespace: devs
          type: Opaque
          stringData:
            password: "{{ sonarqube_admin_password }}"
            currentPassword: "admin"
            jdbc-password: "{{ sonarqube_postgres_password }}"
      register: secret_result
      changed_when: "'created' in secret_result.stdout or 'configured' in secret_result.stdout"

    # =========================================================================
    # Helm — SonarQube
    # =========================================================================
    - name: Aggiungi Helm repo sonarqube
      ansible.builtin.command: helm repo add sonarqube https://SonarSource.github.io/helm-chart-sonarqube
      register: repo_result
      changed_when: "'has been added' in repo_result.stdout"
      failed_when:
        - repo_result.rc != 0
        - "'already exists' not in repo_result.stderr"

    - name: Aggiorna Helm repos
      ansible.builtin.command: helm repo update
      changed_when: false

    - name: Installa SonarQube via Helm
      ansible.builtin.command: >
        helm upgrade --install sonarqube sonarqube/sonarqube
        --namespace devs
        -f {{ sonarqube_k8s_dir }}/sonarqube-values.yml
      register: sonarqube_result
      changed_when: "'STATUS: deployed' in sonarqube_result.stdout"

    - name: Attendi SonarQube ready
      ansible.builtin.command: >
        kubectl rollout status statefulset sonarqube-sonarqube
        --namespace devs --timeout=300s
      changed_when: false

    # =========================================================================
    # Manifest K8s (IngressRoute via Kustomize)
    # =========================================================================
    - name: Applica manifest Kustomize (IngressRoute)
      ansible.builtin.command: kubectl apply -k {{ sonarqube_k8s_dir }}
      register: kustomize_result
      changed_when: "'created' in kustomize_result.stdout or 'configured' in kustomize_result.stdout"

    # =========================================================================
    # Verifica
    # =========================================================================
    - name: Verifica pods SonarQube
      ansible.builtin.command: kubectl get pods -n devs
      register: pods_result
      changed_when: false

    - name: Verifica IngressRoute
      ansible.builtin.command: kubectl get ingressroute -n devs
      register: ingress_result
      changed_when: false

    # =========================================================================
    # Riepilogo
    # =========================================================================
    - name: Riepilogo SonarQube
      ansible.builtin.debug:
        msg:
          - "=== SonarQube Setup Completato ==="
          - ""
          - "URL:        https://sonarqube.oci.santocaruso.eu"
          - "User:       admin"
          - "Password:   (valore in sonarqube-secret)"
          - "PostgreSQL: postgres.database.svc:5432 (database: sonarqube, user: sonarqube)"
          - ""
          - "--- Pods ---"
          - "{{ pods_result.stdout }}"
          - ""
          - "--- IngressRoute ---"
          - "{{ ingress_result.stdout }}"
