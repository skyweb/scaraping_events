kubectl create -f - <<EOF
  apiVersion: tekton.dev/v1
  kind: PipelineRun
  metadata:
    generateName: backoffice-build-
    namespace: tekton-pipelines
  spec:
    pipelineRef: { name: build-and-push }
    params:
      - { name: repo-url, value: "https://github.com/skyweb/scaraping_events.git" }
      - { name: image, value: "eu-milan-1.ocir.io/axbuakdjv1li/events/backoffice:latest" }
      - { name: context, value: "microservices/backoffice-service" }
    workspaces:
      - name: shared-workspace
        volumeClaimTemplate:
          spec: { accessModes: [ReadWriteOnce], resources: { requests: { storage: 1Gi } } }
    taskRunTemplate:
      serviceAccountName: tekton-build
  EOF
